//Rotate 90 degrees a nxn matrix in place.
#include <bits/stdc++.h>
using namespace std;
int main(){
  int n;
  cin >> n;
  int m[n+3][n+3];
  for(int i = 0; i < n; i++)
    for(int j = 0; j < n; j++)
      cin >> m[i][j];
  for(int 
  return 0;
}
